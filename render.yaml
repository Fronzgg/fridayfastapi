services:
  - type: web
    name: friday-cloud
    env: python
    rootDir: onrender-cloud
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    plan: free
